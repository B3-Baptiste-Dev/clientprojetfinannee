import 'package:flutter/cupertino.dart';

class RechercherScreen extends StatelessWidget {
  const RechercherScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  const Center(
      child: Text('Rechercher'),
    );
  }
}